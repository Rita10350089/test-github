 name: Build Reusable
 on:
   workflow_call: #被呼叫
     inputs:
       version: aaa
         required: true
         type: string
         default: "3.10"
 jobs:
   build: #自行取名
     runs-on: ubuntu-latest
     steps:
       -run: echo "Building version ${{ inputs.version}}"

